apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: aggregation-service-db
spec:
  serviceName: aggregation-service-db
  replicas: 1
  selector:
    matchLabels:
      app: aggregation-service-db
  template:
    metadata:
      labels:
        app: aggregation-service-db
    spec:
      containers:
        - name: aggregation-service-db
          image: timescale/timescaledb:latest-pg16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: aggregation-service-db-config
                  key: POSTGRES_DB
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: aggregation-service-db-secret
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: aggregation-service-db-secret
                  key: POSTGRES_PASSWORD
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 5Gi
