package com.pulsegrid.deviceregistry.application.port.in;

import com.pulsegrid.deviceregistry.application.port.result.ListDevicesResult;

public interface ListDevicesUseCase {
    ListDevicesResult listAll();
}
